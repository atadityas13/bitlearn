RewriteEngine On

# Allow direct access to existing files
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Route everything else to index.php with PATH_INFO
RewriteRule ^(.*)$ index.php/$1 [QSA,L]
